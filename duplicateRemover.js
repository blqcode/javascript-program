
function duplicate(a){
a.forEach(i => {
    let b=a.indexOf(i)
    console.log(b)
    if(a.length>=0){
        if(b>=a.length){

        
        //compare the element of the array
        if(b==b++){
            a.splice(b)
        }
        else{
            return;
        }
    }
        
}
});
}
duplicate([1,2,3,1,2,4])
Array